# Llavon IME 網站

拉風輸入法的 Nuxt 4 網站。

## 開發

需要 Node.js 22 或更新版本。

```bash
npm install
npm run dev
```

開啟 `http://localhost:3000`。

## 建置

```bash
npm run build
npm run generate
```

`generate` 會產生可部署到靜態網站主機的 `.output/public`。
